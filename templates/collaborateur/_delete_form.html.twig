<form method="post" class="grid justify-items-end mt-4" action="{{ path('app_collaborateur_delete', {'id': collaborateur.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer ce collaborateur ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ collaborateur.id) }}">
    <button class="p-2 items-center bg-red-600 rounded-xl font-semibold text-lg text-white hover:bg-yellow-300 hover:text-red-500">
        Supprimer
    </button>
</form>
