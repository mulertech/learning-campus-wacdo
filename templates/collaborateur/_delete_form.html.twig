<form method="post" action="{{ path('app_collaborateur_delete', {'id': collaborateur.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer ce collaborateur ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ collaborateur.id) }}">
    <button class="btn">Supprimer</button>
</form>
