<div class="grid justify-items-end mt-4">
    <a href="{{ path('app_affectation_delete_confirm', {'id': affectation.id}) }}"
       data-turbo-frame="modal"
       class="p-2 items-center bg-red-600 rounded-xl font-semibold text-lg text-white hover:bg-yellow-300 hover:text-red-500">
        Supprimer
    </a>
</div>
