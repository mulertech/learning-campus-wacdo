{% extends 'base.html.twig' %}

{% block title %}Fonction{% endblock %}

{% block body %}
    <h1 class="text-center text-4xl text-shadow-lg font-medium m-8 text-white"><span class="text-yellow-300">F</span>onction</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Intitule</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>{{ fonction.id }}</td>
                <td>{{ fonction.intitule }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_fonction_index') }}">Retourner à la liste</a>

    <a href="{{ path('app_fonction_edit', {'id': fonction.id}) }}">Modifier</a>

    {{ include('fonction/_delete_form.html.twig') }}
{% endblock %}
